import 'dart:ffi';

/*
void Second_highest_lowest(int array_nums[],int array_size){
  if(array_size == 2){
    if (array_nums[0] < array_nums[1]) {
      print("${array_nums[0]} " " ${array_nums[1]}");
    }else{
       print("${array_nums[1]} " " ${array_nums[0]}");
    }
  }
  else{
    bool flag;
    int temp;

    do{
      flag = false;

      for (int x = 0; x < ){
        if (array_nums[x] > array_nums[x + 1]) {
          temp = array_nums[x];
          array_nums[x] = array_nums[x+1];
          array_nums[x+1] = temp;
          flag = true;
        }
      }
    }while (flag); 
  int index = 0;
  int index2 = array_size - 1;
  for (int y = 0; y < array_size-1;y++){
    if (array_nums[y] == array_nums[y + 1]){
      index++;
    }
    else{
      break;
    }
  }
  for (int z =array_size - 1; array_size > 0;z--){
    if(array_nums[z] == array_nums[z - 1]){
      if (array_nums[z] == array_nums[z - 1]) {
        index2--
      }else{
        break;
      }
    }
  }
  }
}
*/
void main() {
  int num1 = [1, 2, 3, 4, 12, 12, 39, 44, 50, 15, 9, 8, 60, 72] as int;
  int size_A = num1.bitLength;
}
